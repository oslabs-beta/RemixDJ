import type { Plugin } from "esbuild";
import type { CompileOptions } from "../options";
import type { RemixConfig } from "../../config";
export declare const cssModulesPlugin: ({ config, mode, outputCss, }: {
    config: RemixConfig;
    mode: CompileOptions["mode"];
    outputCss: boolean;
}) => Plugin;
