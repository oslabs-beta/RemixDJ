import type { RemixConfig } from "./config";
import type { CompileResult } from "./compiler";
export type Update = {
    id: string;
    url: string;
    revalidate: boolean;
    reason: string;
};
export declare let updates: (config: RemixConfig, result: CompileResult, prevResult: CompileResult) => Update[];
